#include <stdio.h>

int main ()

{
    int a,b,c;
    printf ("Enter the first number then second number\n");
    scanf ("%d%d",&a,&b);
    c=a+b;
    printf ("%d+%d equals to %d",a,b,c);
    return 0;
}
